// import { Request, Response } from 'express';
// import ballsService from '../services/ballsService';

// const ballsController = {
//   async getBalls(req: Request, res: Response) {
//     try {
//       const balls = await ballsService.getBalls();
//       res.status(200).json({ ball_name: balls });
//     } catch (error) {
//       console.error('Error fetching balls:', error);
//       res.status(500).json({ error: 'Internal server error' });
//     }
//   }
// };

// export default ballsController;
