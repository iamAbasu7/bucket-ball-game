// import { Request, Response } from 'express';
// import bucketsService from '../services/bucketsService';

// const bucketsController = {
//   async getBuckets(req: Request, res: Response) {
//     try {
//       const buckets = await bucketsService.getBuckets();
//       res.status(200).json({ bucket_names: buckets });
//     } catch (error) {
//       console.error('Error fetching bucket names:', error);
//       res.status(500).json({ error: 'Internal server error' });
//     }
//   }
// };

// export default bucketsController;
